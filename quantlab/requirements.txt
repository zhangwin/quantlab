# QuantLab dependencies
# NOTE: This file is superseded by pyproject.toml for pip-install users.
#       Prefer: pip install -e .  (from repo root)
# Install with: pip install -r quantlab/requirements.txt

# --- Qlib (install from local source: pip install -e ./qlib) ---
# Qlib has Cython extensions, needs build tools
cython
setuptools>=64
setuptools-scm

# --- Qlib's own dependencies (from qlib/pyproject.toml) ---
numpy>=1.24
pandas>=1.1
pyyaml
mlflow
filelock>=3.16.0
redis
dill
fire
ruamel.yaml>=0.17.38
python-redis-lock
tqdm
pymongo
loguru
lightgbm
gym
cvxpy
joblib
matplotlib
pyarrow
pydantic-settings

# --- Data download ---
yfinance>=0.2.18
requests
baostock  # A股数据源（免费，无频率限制）

# --- Visualization (M1.5) ---
plotly>=5.15

# --- Testing ---
pytest>=7.0

# --- Computation ---
scipy>=1.10
